# 算法科研中的模型、基线与实验管理规范

## 1. 文档目的

本规范用于改善当前算法科研中以下常见问题：

- 调整参数后直接上传服务器运行，缺少统一实验入口；
- 实验结果主要依赖零散日志，难以系统比较；
- 无法快速确认某个结果对应的代码、数据和参数版本；
- 模型、损失函数、数据处理和超参数同时变化，难以判断性能变化来源；
- 基线模型实现不断被覆盖，历史实验失去可比性；
- 实验数量不断增加，但缺少明确的研究问题、结论和下一步方向；
- 服务器、个人电脑与代码仓库之间的工作流不统一；
- 论文中的主实验、基线、消融和参数实验没有提前形成结构。

本规范的核心目标是将当前的工作方式：

> 修改代码 → 上传服务器 → 运行 → 下载日志 → 再修改

转变为：

> 提出问题 → 明确假设 → 设计对照 → 固定条件 → 运行实验 → 记录结果 → 得出结论 → 决定下一步

最终需要保证每一个重要实验都可以被追踪、复现、比较和解释。

---

## 2. 核心原则

### 2.1 实验必须服务于研究问题

每次正式实验开始前，应明确当前实验试图回答什么问题。

不建议以以下方式定义实验：

- 尝试更大的模型；
- 再调整一下学习率；
- 加一个注意力模块看看；
- 换一种损失函数试试。

建议改为可验证的问题：

- 当前模型输出过度平滑，是否因为普通 MSE 鼓励模型学习条件均值？
- 加入一阶差分损失后，能否提高预测序列的动态变化一致性？
- LSTM 是否稳定优于不建模时间依赖的 MLP？
- 图结构编码器是否提供了普通全连接网络无法获得的信息？
- 当前复杂模型是否真正优于训练集均值、线性回归和简单神经网络基线？
- 当前性能提升是否来自模型结构，而不是数据划分或预处理变化？

每个实验都应尽量对应一个可以被支持或否定的假设。

---

### 2.2 一次实验只改变一个主要因素

为了判断性能变化来源，一次实验原则上只改变一个主要因素，例如：

- 模型结构；
- 损失函数；
- 数据输入；
- 预处理方法；
- 学习率；
- 隐藏层维度；
- 时间窗口长度；
- 训练策略。

其他条件尽量保持不变。

如果一次同时改变模型结构、损失函数、数据划分和学习率，即使结果提高，也无法确认真正有效的因素。

允许多个因素同时变化的情况包括：

1. 完整模型与完整基线的总体比较；
2. 一个模块本身需要配套的训练策略；
3. 工程性重构，但必须验证重构前后结果一致；
4. 明确标记为探索性实验，而不是结论性实验。

---

### 2.3 代码、数据、配置和结果必须绑定

任何一个正式实验，都需要绑定以下信息：

- 代码版本；
- 数据版本；
- 数据划分版本；
- 预处理版本；
- 实验配置；
- 随机种子；
- 运行环境；
- 模型输出；
- 评价指标；
- 最终结论。

仅保留一个 `.log` 文件，不能视为一个完整实验记录。

---

### 2.4 基线必须长期稳定

基线不是临时写出的一个模型文件，而是一套固定的实验协议。

一个完整基线至少包括：

- 模型定义；
- 输入和输出；
- 数据划分；
- 预处理流程；
- 损失函数；
- 训练设置；
- 随机种子；
- 评价指标；
- 模型选择标准；
- 测试集评估方式。

已经用于正式比较的基线不得直接覆盖修改。

如果发现旧基线实现不合理，应创建新版本，例如：

```text
baseline_lstm_v1
baseline_lstm_v2
```

旧版本应继续保留，以维持历史结果的可解释性。

---

### 2.5 实验完成必须包含结论

训练任务运行结束，不代表实验结束。

每次实验最终必须归入以下状态之一：

- `KEEP`：结果支持保留该修改；
- `REJECT`：没有明显收益，或副作用大于收益；
- `INCONCLUSIVE`：证据不足，需要补充实验；
- `FAILED`：由于代码、数据或运行错误，结果无效；
- `ARCHIVED`：早期探索实验，不再作为当前研究依据。

实验还应填写：

- 主要发现；
- 是否符合预期；
- 是否需要更多随机种子；
- 是否需要补充可视化；
- 是否进入下一阶段；
- 下一步实验是什么。

---

## 3. 研究问题树

在模型开发前，建议先建立一棵研究问题树，以避免模型结构、调参和数据问题同时混杂。

以“由结构连接预测动态功能连接序列”为例，可以分为以下层级。

### 3.1 第一层：任务是否存在可学习信号

优先建立以下低复杂度基线：

- 训练集目标均值；
- 静态 FC 在时间维度上的重复；
- 个体平均 FC；
- 线性回归；
- Ridge 或 Lasso；
- 简单 MLP；
- 随机打乱输入和目标被试对应关系的控制实验。

需要回答：

- 复杂模型是否明显优于目标均值？
- 输入中是否包含个体差异信息？
- 输入和目标之间的对应关系被打乱后，性能是否显著下降？
- 当前评价指标是否过度奖励平滑输出？
- 单一静态结构连接是否足以预测具体的动态 FC 序列？

如果复杂模型与均值基线接近，应优先排查任务定义和评价指标，而不是继续增加模型复杂度。

---

### 3.2 第二层：模型是否学到了时间变化

不能只使用整体 MSE 或 MAE 判断时序模型是否有效，还应关注：

- 预测序列方差；
- 一阶差分误差；
- 一阶差分相关；
- 二阶差分误差；
- 动态相关；
- 频谱相似性；
- 功率谱密度差异；
- 峰值位置和幅度；
- 状态切换次数；
- 状态持续时间；
- 窗口间变化幅度；
- 动态 FC 分布相似性。

需要回答：

- 模型是否只是学习到平均值？
- 预测结果是否有合理波动？
- 预测波动是否与真实序列同步？
- 动态改善是否以整体重构误差显著恶化为代价？

---

### 3.3 第三层：复杂模型是否必要

建议建立由简单到复杂的模型阶梯：

```text
B0：训练集均值或静态 FC 复制
B1：线性回归 / Ridge
B2：简单 MLP
B3：LSTM / GRU
B4：TCN / 基础 Transformer
B5：结构连接条件时序模型
B6：完整提出模型
```

复杂模型应稳定优于简单模型后，才进入正式方法比较。

不能仅根据单次最好结果判断复杂模型有效。

---

### 3.4 第四层：提出模型的模块是否有效

假设完整模型包括：

- 结构图编码器；
- 时间序列解码器；
- 个体嵌入；
- 一阶差分损失；
- 二阶差分损失；
- 频域约束。

则至少需要以下消融：

```text
完整模型
去掉结构图编码器
去掉时间模块
去掉个体嵌入
去掉一阶差分损失
去掉二阶差分损失
去掉频域约束
```

消融实验应使用相同数据划分、训练预算和评价指标。

---

## 4. 实验分级

不是每一个想法都需要直接进行完整训练。建议将实验分为三个等级。

### 4.1 Level 0：代码与流程检查

目的：

- 确认代码可以运行；
- 检查输入输出维度；
- 检查 loss 是否下降；
- 检查梯度是否正常；
- 检查保存和加载逻辑；
- 检查预测结果是否存在 NaN 或常数输出。

建议设置：

- 少量样本；
- 1 个随机种子；
- 2～5 个 epoch；
- 不进行正式结果比较。

Level 0 结果不得作为论文证据。

---

### 4.2 Level 1：方向筛选

目的：

- 判断一个修改是否值得继续；
- 快速排除明显无效的方法；
- 初步判断超参数范围。

建议设置：

- 完整数据或代表性子集；
- 1～3 个随机种子；
- 较少训练轮数；
- 固定验证集；
- 主要关注方向性差异。

Level 1 结果可以指导下一步，但通常不直接作为最终论文结果。

---

### 4.3 Level 2：正式确认

目的：

- 形成论文主结果；
- 稳定比较模型；
- 支持方法有效性结论。

建议设置：

- 完整数据；
- 固定的数据划分；
- 多个随机种子；
- 完整训练；
- 统一 early stopping；
- 均值和标准差；
- 置信区间或显著性分析；
- 完整预测结果与可视化；
- 必要的消融和敏感性分析。

---

## 5. 实验命名规范

建议每个实验拥有唯一 ID，例如：

```text
E0001
E0002
E0003
```

实验名称使用小写英文和下划线：

```text
E0001_mean_baseline
E0002_ridge_baseline
E0003_lstm_baseline
E0004_lstm_diff1_loss
E0005_lstm_diff1_weight_02
```

建议避免以下命名：

```text
test1
new_model
final
final2
final_final
best_model
try_again
```

实验 ID 一旦创建，不重复使用。

---

## 6. 推荐项目目录

```text
project/
├── configs/
│   ├── data/
│   │   ├── data_v1.yaml
│   │   └── split_v1.yaml
│   ├── model/
│   │   ├── ridge.yaml
│   │   ├── mlp.yaml
│   │   ├── lstm.yaml
│   │   └── transformer.yaml
│   ├── loss/
│   │   ├── mse.yaml
│   │   ├── mse_diff1.yaml
│   │   └── mse_diff1_diff2.yaml
│   ├── trainer/
│   │   ├── debug.yaml
│   │   ├── screening.yaml
│   │   └── formal.yaml
│   └── experiment/
│       ├── E0001_mean_baseline.yaml
│       ├── E0002_ridge_baseline.yaml
│       ├── E0003_lstm_baseline.yaml
│       └── E0004_lstm_diff1_loss.yaml
├── src/
│   ├── datasets/
│   ├── models/
│   ├── losses/
│   ├── metrics/
│   ├── trainers/
│   └── utils/
├── scripts/
│   ├── train.py
│   ├── evaluate.py
│   ├── predict.py
│   ├── compare_runs.py
│   └── submit.sh
├── tests/
├── reports/
│   ├── experiment_registry.csv
│   ├── experiment_notes/
│   ├── tables/
│   └── figures/
├── outputs/
│   └── E0004/
│       ├── config.yaml
│       ├── metadata.json
│       ├── metrics.json
│       ├── train.log
│       ├── checkpoints/
│       ├── predictions/
│       └── figures/
├── README.md
├── CHANGELOG.md
├── requirements.txt
└── .gitignore
```

其中：

- `configs/model/`：描述模型结构；
- `configs/loss/`：描述损失函数；
- `configs/data/`：描述数据与划分；
- `configs/trainer/`：描述训练等级与训练预算；
- `configs/experiment/`：组合一次完整实验；
- `outputs/`：保存自动生成的实验结果；
- `reports/experiment_registry.csv`：保存面向科研判断的实验主表。

---

## 7. 实验配置规范

建议使用 YAML 管理配置。

示例：

```yaml
experiment:
  id: E0004
  name: lstm_diff1_loss
  level: screening
  hypothesis: 一阶差分损失可以减少预测序列均值化
  primary_change: 增加一阶差分损失
  baseline: E0003
  owner: researcher_name
  status: planned

data:
  dataset_version: data_v1
  split_version: split_v1
  preprocessing_version: preprocess_v1
  target: dynamic_fc
  input: structural_connectivity

model:
  name: lstm
  hidden_size: 128
  num_layers: 2
  dropout: 0.1

loss:
  name: mse_diff1
  mse_weight: 1.0
  diff1_weight: 0.2

training:
  epochs: 100
  batch_size: 16
  learning_rate: 0.001
  weight_decay: 0.0001
  early_stopping_patience: 15
  seeds:
    - 42
    - 43
    - 44

evaluation:
  primary_metric: validation_dynamic_correlation
  metrics:
    - mse
    - mae
    - pearson_r
    - temporal_variance
    - diff1_mae
    - diff1_correlation

decision_rule:
  keep_if:
    - validation_dynamic_correlation improves
    - mae degradation is less than 5 percent
```

配置文件应完整描述实验，不依赖运行时手工补充大量参数。

---

## 8. 每次运行必须自动记录的信息

### 8.1 实验身份

- experiment_id；
- run_id；
- 实验名称；
- 开始时间；
- 结束时间；
- 运行状态；
- 所属实验等级；
- 服务器名称；
- GPU 型号。

### 8.2 代码版本

- Git commit hash；
- Git branch；
- 是否存在未提交修改；
- 当前代码仓库地址；
- Python 版本；
- CUDA 版本；
- PyTorch 或其他框架版本；
- 依赖环境导出文件。

如果存在未提交代码，应输出警告，并在正式实验中禁止运行或自动保存 diff。

### 8.3 数据版本

- 数据集名称；
- 数据版本；
- 数据路径；
- 样本量；
- 训练、验证、测试样本数；
- 划分版本；
- 预处理版本；
- 数据文件校验值；
- 是否存在缺失样本；
- 是否存在被试泄漏。

### 8.4 配置参数

- 模型结构；
- 参数量；
- 输入输出维度；
- 损失函数；
- 优化器；
- 学习率；
- batch size；
- epoch；
- early stopping；
- 随机种子；
- 正则化参数；
- 数据增强；
- 训练精度类型。

### 8.5 训练过程

- train loss；
- validation loss；
- 学习率变化；
- 梯度范数；
- 每轮耗时；
- GPU 显存；
- 最佳 epoch；
- early stopping 触发位置。

### 8.6 最终输出

- 最佳 checkpoint；
- 最后一个 checkpoint；
- 预测结果；
- 样本级评价指标；
- 总体评价指标；
- 各随机种子结果；
- 均值与标准差；
- 预测曲线；
- 误差分布；
- 时序动态指标；
- 训练曲线。

---

## 9. 输出目录规范

每个实验输出目录建议具有相同结构：

```text
outputs/E0004/seed_42/
├── config_resolved.yaml
├── metadata.json
├── environment.txt
├── git_diff.patch
├── train.log
├── metrics_best.json
├── metrics_last.json
├── checkpoints/
│   ├── best.pt
│   └── last.pt
├── predictions/
│   ├── validation_predictions.npz
│   └── test_predictions.npz
└── figures/
    ├── loss_curve.png
    ├── prediction_examples.png
    └── error_distribution.png
```

`config_resolved.yaml` 应保存最终实际生效的全部参数，而不只是用户手工填写的部分。

---

## 10. 实验主表

即使使用 MLflow、W&B 等平台，也建议保留一份人工维护的实验主表。

推荐字段：

| 字段 | 说明 |
|---|---|
| Experiment ID | 唯一实验编号 |
| Date | 实验日期 |
| Research Question | 当前研究问题 |
| Hypothesis | 实验假设 |
| Primary Change | 本次主要改动 |
| Baseline | 对照实验 |
| Dataset Version | 数据版本 |
| Split Version | 划分版本 |
| Code Commit | 代码版本 |
| Seeds | 随机种子 |
| Primary Metric | 主要指标 |
| Main Result | 核心结果 |
| Side Effects | 副作用 |
| Status | KEEP / REJECT / INCONCLUSIVE 等 |
| Conclusion | 实验结论 |
| Next Step | 下一步 |
| Notes | 其他说明 |

示例：

| ID | 问题 | 改动 | 对照 | 核心结果 | 结论 | 下一步 |
|---|---|---|---|---|---|---|
| E0001 | 均值预测有多强 | 输出训练集均值 | 无 | MAE=0.12 | 基准 | 训练线性模型 |
| E0002 | SC 是否含个体信息 | Ridge | E0001 | MAE 提升 3% | KEEP | 比较 MLP |
| E0003 | LSTM 是否优于 MLP | 加入时间解码 | E0002 | 无稳定提升 | REJECT | 检查时序目标 |
| E0004 | 差分损失能否减少平滑 | 加 diff1 loss | E0003 | 动态相关提高 | KEEP | 搜索损失权重 |

实验平台负责回答：

> 具体运行了什么？

实验主表负责回答：

> 为什么运行，以及科研上得到了什么结论？

---

## 11. Git 管理规范

### 11.1 分支建议

```text
main
develop
feature/model_name
feature/loss_name
experiment/E0004
fix/data_leakage
```

个人科研项目也可以简化为：

```text
main
experiment/*
fix/*
```

### 11.2 提交规范

提交信息示例：

```text
feat(model): add lstm temporal decoder
feat(loss): add first-order difference loss
fix(data): prevent subject leakage in split
refactor(train): move parameters into yaml config
exp(E0004): add lstm diff1 experiment config
docs: update experiment registry
```

每次正式实验开始前应：

1. 提交当前代码；
2. 确认工作区干净；
3. 记录 commit hash；
4. 再提交服务器任务。

### 11.3 不应进入 Git 的内容

- 大型模型 checkpoint；
- 原始数据；
- 大型预测文件；
- 缓存文件；
- 临时日志；
- 本地环境目录；
- 密钥和账号信息。

这些文件应通过 `.gitignore` 排除。

---

## 12. 数据和划分管理

### 12.1 固定数据版本

数据处理流程变化后应创建新版本：

```text
data_v1
data_v2
preprocess_v1
preprocess_v2
split_v1
split_v2
```

不要在不改变名称的情况下直接覆盖旧数据。

### 12.2 固定正式划分

模型比较阶段应固定训练集、验证集和测试集。

测试集只能用于最终确认，不应在调参过程中反复查看。

### 12.3 防止数据泄漏

需要重点检查：

- 同一被试是否同时进入训练和测试；
- 同一被试不同时间窗口是否被拆分到不同集合；
- 标准化参数是否仅使用训练集估计；
- 特征选择是否使用了测试集信息；
- 数据增强是否跨集合重复；
- 动态 FC 相邻窗口是否造成近重复泄漏。

### 12.4 数据校验值

关键数据文件建议记录：

- 文件大小；
- 修改日期；
- MD5 或 SHA256；
- 样本数；
- 特征维度。

这样可以避免“文件名相同但内容已经变化”。

---

## 13. 基线管理规范

### 13.1 基线类别

建议至少包括：

#### 统计基线

- 全局均值；
- 训练集均值；
- 被试均值；
- 最近值复制；
- 静态目标复制。

#### 传统机器学习基线

- 线性回归；
- Ridge；
- Lasso；
- PCA + Ridge；
- 随机森林或 XGBoost，在任务适用时使用。

#### 简单神经网络基线

- MLP；
- 1D CNN；
- GRU；
- LSTM；
- 简单 TCN；
- 基础 Transformer。

#### 任务相关已有方法

- 相关论文中的代表性模型；
- 与当前输入输出设定最接近的方法；
- 当前领域公认的标准方法。

---

### 13.2 基线公平性

所有基线应尽量使用：

- 相同数据划分；
- 相同输入信息；
- 相同评价指标；
- 合理且相近的训练预算；
- 相同模型选择规则；
- 相同随机种子；
- 相同测试协议。

不能故意使用明显不合理的基线参数。

复杂模型参数量明显更大时，应补充：

- 参数量；
- FLOPs；
- 训练时间；
- 推理时间；
- 显存占用。

---

### 13.3 基线冻结

一个基线进入正式比较后，应冻结以下内容：

- 实现代码；
- 配置文件；
- 数据版本；
- 训练协议；
- 评价方式。

需要修改时，创建新版本而不是覆盖。

---

## 14. 超参数管理

### 14.1 先搜索关键参数

优先搜索对模型机制影响最大的参数，例如：

- 学习率；
- 损失权重；
- 隐藏层维度；
- 层数；
- dropout；
- 时间窗口长度；
- 图卷积层数；
- 注意力头数。

不建议一开始同时搜索大量低影响参数。

### 14.2 分阶段搜索

建议流程：

1. 使用 Level 0 检查参数范围是否可运行；
2. 使用 Level 1 粗搜索；
3. 锁定合理范围；
4. 使用多个随机种子确认；
5. 只对最终候选进行 Level 2 正式训练。

### 14.3 防止对测试集调参

所有超参数选择都应基于验证集。

测试集仅用于最终模型的独立评估。

### 14.4 保存完整搜索空间

每次参数搜索应保存：

- 搜索参数；
- 搜索范围；
- 搜索方法；
- 试验次数；
- 随机种子；
- 最佳参数；
- 所有候选结果；
- 选择最佳模型的规则。

---

## 15. 服务器运行规范

建议的服务器流程：

```text
本地创建研究问题和实验配置
        ↓
本地完成代码修改
        ↓
Git commit
        ↓
服务器 git pull
        ↓
通过配置文件提交实验
        ↓
训练脚本自动记录版本、参数和指标
        ↓
结果写入统一输出目录和实验平台
        ↓
本地查看与比较
        ↓
填写实验结论和下一步
```

建议运行命令：

```bash
python scripts/train.py \
  --config configs/experiment/E0004_lstm_diff1_loss.yaml
```

不建议长期使用：

```bash
python train.py \
  --lr 0.001 \
  --hidden 128 \
  --loss mse_diff \
  --dropout 0.1
```

后一种方式容易导致运行结束后无法确认实际参数。

---

## 16. 日志与异常管理

### 16.1 日志内容

日志应同时输出到：

- 终端；
- 文件；
- 实验管理平台。

关键日志包括：

- 实验 ID；
- 数据规模；
- 模型参数量；
- 每轮 loss；
- 验证指标；
- 最佳 epoch；
- 当前学习率；
- 保存 checkpoint 的位置；
- 异常信息。

### 16.2 异常实验

以下情况应自动将实验标记为 `FAILED`：

- loss 为 NaN；
- 梯度爆炸；
- 数据为空；
- 输入输出维度不一致；
- checkpoint 保存失败；
- 训练过程中断；
- 指标文件未生成。

失败实验也应保留配置和错误日志，避免重复踩坑。

---

## 17. 评价指标规范

评价指标应分为三类。

### 17.1 主要指标

用于决定模型是否更好。

每个阶段只能提前指定少量主要指标，避免结果出来后挑选对自己最有利的指标。

### 17.2 辅助指标

用于解释模型行为，例如：

- 模型是否过度平滑；
- 是否牺牲局部动态换取整体误差；
- 是否只在部分被试上有效；
- 是否只改善特定时间段。

### 17.3 工程指标

包括：

- 参数量；
- 训练时间；
- 推理时间；
- 显存占用；
- 收敛速度；
- 失败率。

---

## 18. 随机种子和统计报告

探索阶段可以使用少量随机种子。

正式结果建议至少报告：

- 多个随机种子的均值；
- 标准差；
- 每个种子的原始结果；
- 必要时提供置信区间；
- 与主要基线的配对比较。

不能只报告表现最好的随机种子。

如果不同随机种子差异很大，应将其视为模型稳定性问题，而不是继续挑选最佳结果。

---

## 19. 工具建议

建议逐步引入工具，而不是一次性搭建过重系统。

### 第一阶段

```text
Git + YAML + CSV 实验主表
```

目标：

- 规范代码版本；
- 统一配置；
- 建立实验编号；
- 形成可比较记录。

### 第二阶段

```text
Git + YAML + MLflow
```

目标：

- 自动记录参数、指标和文件；
- 在网页中比较实验；
- 管理 checkpoint 和预测结果。

### 第三阶段

```text
Git + Hydra + MLflow
```

目标：

- 模块化配置；
- 支持批量实验；
- 减少配置重复；
- 管理参数组合。

### 第四阶段

```text
Git + Hydra + MLflow + DVC
```

适用于：

- 数据版本频繁变化；
- 大文件较多；
- 多人协作；
- 不同实验依赖不同数据版本。

个人科研早期不建议一次性全部引入。

---

## 20. 当前项目的优先落地顺序

建议先完成以下工作，再继续增加新模型。

### 阶段一：整理现状

1. 列出现有重要模型；
2. 列出现有数据版本；
3. 列出现有正式和非正式实验；
4. 找出无法确认代码或参数的孤立结果；
5. 将孤立结果标记为归档，不作为正式证据。

### 阶段二：固定基线

建立至少以下基线：

1. 目标均值基线；
2. 线性或 Ridge 基线；
3. MLP 基线；
4. LSTM 或当前简单时序模型；
5. 当前主模型。

### 阶段三：统一配置

1. 将模型参数迁移至 YAML；
2. 将损失参数迁移至 YAML；
3. 将数据路径和版本迁移至 YAML；
4. 将训练设置迁移至 YAML；
5. 建立实验配置文件。

### 阶段四：自动记录

训练脚本自动保存：

- Git commit；
- 完整配置；
- 环境信息；
- 训练曲线；
- 最佳指标；
- checkpoint；
- 预测结果；
- 图像。

### 阶段五：建立科研决策流程

每次实验结束后填写：

- 结果；
- 结论；
- 状态；
- 下一步。

---

## 21. 建议 Codex 进一步讨论和实现的问题

可以让 Codex 基于本规范继续完成以下任务。

### 21.1 项目诊断

请检查当前项目并回答：

1. 当前代码结构是否支持配置驱动实验？
2. 哪些参数仍然硬编码在训练脚本中？
3. 当前数据划分是否可追踪？
4. 当前模型和损失是否可以独立组合？
5. 当前日志是否足以复现实验？
6. 是否存在训练集、验证集和测试集泄漏风险？
7. 当前 checkpoint、预测结果和图像如何保存？
8. 哪些代码需要先重构，哪些可以暂时保留？

### 21.2 最小重构方案

请优先实现：

1. `configs/` 配置系统；
2. 统一的 `train.py` 入口；
3. 实验 ID；
4. 自动输出目录；
5. Git commit 记录；
6. 随机种子固定；
7. 指标 JSON 保存；
8. checkpoint 统一保存；
9. 实验主表更新；
10. 基础 MLflow 接入。

### 21.3 基线实现

请检查并实现：

1. 训练集均值基线；
2. 线性回归或 Ridge；
3. MLP；
4. LSTM；
5. 当前完整模型；
6. 相同数据与评价接口；
7. 相同测试协议；
8. 参数量和运行时间统计。

### 21.4 测试

需要补充：

- 配置加载测试；
- 数据划分测试；
- 数据泄漏测试；
- 模型输入输出维度测试；
- loss 数值测试；
- checkpoint 保存加载测试；
- 固定随机种子复现测试；
- 小数据过拟合测试；
- 指标计算测试。

---

## 22. 可直接提供给 Codex 的讨论提示词

```text
我正在整理一个算法科研项目，目前的问题是模型、基线、实验配置和服务器运行记录比较混乱。请你阅读《算法科研中的模型、基线与实验管理规范》，结合当前代码仓库进行项目诊断和重构规划。

你的任务不是立即大规模修改代码，而是先完成以下内容：

1. 阅读当前项目目录、训练入口、模型实现、数据处理、损失函数、评价指标和服务器脚本；
2. 说明当前实验不可复现或难以比较的具体原因；
3. 区分“必须立即修改”“建议后续修改”和“暂时不用修改”的内容；
4. 设计一个最小可落地的配置驱动实验系统；
5. 给出新的项目目录结构；
6. 设计 experiment_id、run_id、配置文件和输出目录规范；
7. 说明如何自动记录 Git commit、数据版本、随机种子、环境、参数、指标和 checkpoint；
8. 设计均值、Ridge、MLP、LSTM 和当前模型的统一基线接口；
9. 设计实验主表及其自动或半自动更新方式；
10. 给出分阶段实施计划，优先保证现有代码仍可运行；
11. 每个阶段列出需要新增、修改和迁移的文件；
12. 对不确定之处先指出，不要自行假设数据格式或训练流程；
13. 在我确认方案后，再逐步实现代码。

重构时请遵循以下原则：

- 不直接覆盖已经得到正式结果的旧基线；
- 一次只做一个可验证的重构步骤；
- 每一步都提供运行方法和验证方式；
- 优先做最小可用系统，不要一次引入过多平台；
- 第一阶段优先使用 Git、YAML 和 CSV；
- MLflow、Hydra 和 DVC 分阶段加入；
- 保留对旧命令和旧配置的兼容性，或提供明确迁移说明；
- 所有正式实验都必须能够从配置文件复现；
- 所有输出都必须绑定代码、数据、参数和随机种子版本。
```

---

## 23. 最终判断标准

当实验管理系统初步完成后，应能够快速回答以下问题：

1. 某个结果由哪版代码产生？
2. 使用了哪份数据和哪种划分？
3. 使用了什么参数和随机种子？
4. 与哪个基线进行比较？
5. 本次实验只改变了什么？
6. 结果是否稳定？
7. 为什么保留或否定这个改动？
8. 下一步实验是什么？
9. 是否可以一条命令重新运行？
10. 论文表格中的结果能否追溯到具体实验目录？

如果这些问题仍然需要依靠记忆、聊天记录或翻找多个日志才能回答，说明实验管理流程还没有真正建立。
